[project]
name = "distributed-gpt-trainer"
version = "0.1.0"
requires-python = ">=3.13"

dependencies = [
    "torch",
    "numpy",
]

[dependency-groups]
dev = [
    "pytest",
    "ruff",
    "pyright",
]

[tool.setuptools.package-data]
dgpt = ["resources/*.txt"]
